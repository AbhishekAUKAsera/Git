//Here we are learning the concept of staging 
//the link for video is  https://www.youtube.com/watch?v=z5jZ9lrSpqk&t=44s
public class Abc {
  public static void getid(){
    
    Id AccountID = [SELECT ID FROM Account].Id;

    Account acc = [SELECT Id, Name FROM Account];

    if(acc.Id != null){
      acc.Name = 'Learn Git tutorial Properly';

      update acc;
    }

  }
}
